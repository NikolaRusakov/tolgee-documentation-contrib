Before you ship, read the [production guide](/android-sdk/production). It covers one Content Delivery URL per
environment, cache invalidation with the version code, and what the app shows while offline. The Content Delivery URL
is public and may live in the app; Tolgee API keys are used by the Tolgee CLI only and never enter the APK.
