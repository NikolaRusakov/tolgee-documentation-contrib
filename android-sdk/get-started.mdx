---
id: get-started
title: Get started with Tolgee Android SDK
sidebar_label: Get started
description: "Choose how Android translations reach your app, bundled or over the air, then open the Tolgee Android SDK guide for your UI toolkit."
image: /img/og-images/android-sdk.png
keywords: [tolgee, android sdk, get started, over the air, bundled translations, tolgee cli, android views, jetpack compose]
---

import FaqSection from '@site/src/component/docs/FaqSection';

Tolgee delivers Android translations two ways: bundled in the APK with the Tolgee CLI, or over the air through a
Content Delivery read by Tolgee Android SDK. This page is for anyone starting an integration. It settles that choice,
points you at the guide for your UI toolkit, and lists what to prepare in the Tolgee Platform before you change any
code.

## Do you need over-the-air updates?

This is the first decision, because it decides whether you add Tolgee Android SDK at all. **Bundled** translations
ship inside the APK: the Tolgee CLI writes them into `res/values-xx/strings.xml` and a corrected text needs a new
release. **Over the air**, a Content Delivery serves the translations at runtime, so a fix reaches installed apps
within minutes. Both keep `strings.xml` in the app as the offline fallback.

A second, independent decision is where the keys themselves come from. The two combine into four paths:

| Keys come from | Bundled in the APK | Over the air |
|---|---|---|
| **`strings.xml` in your repository** | Push with the CLI, translate on the platform, `tolgee pull` before each release | Push with the CLI, the Content Delivery serves JSON, the app reads by resource id |
| **The Tolgee Platform** (web app, REST API or MCP server) | Create keys on the platform, `tolgee pull` generates `values-xx/strings.xml` | Create keys on the platform, the Content Delivery serves JSON, the app reads by key name |

The **column** decides whether you install the SDK. The **row** decides only how keys reach Tolgee, and you can change
it later without touching app code.

Choosing bundled? You do not need Tolgee Android SDK at all. The whole workflow is two Tolgee CLI commands, run
from the app module:

```bash
tolgee push   # send res/values/strings.xml to the platform
tolgee pull   # write the translations back before a release
```

The [Tolgee CLI push and pull](/tolgee-cli/push-pull-strings) page covers the configuration. Everything below covers
the over-the-air column.

## Which guide should I follow?

| Your app | Open this guide |
|---|---|
| Activities, Fragments and XML layouts, with or without a ViewModel | [Install Tolgee Android SDK in an Android Views app](/android-sdk/integrations/android-views/installation) |
| Jetpack Compose screens | [Install Tolgee Android SDK in a Jetpack Compose app](/android-sdk/integrations/jetpack-compose/installation) |
| Both toolkits in one app | Follow the Compose guide, then the "wrap the Activity context" step of the Views guide |
| An existing app with hundreds of `getString` calls | Install first, then [migrate the existing app](/android-sdk/migrate-existing-app) pattern by pattern |
| Compose Multiplatform | The Compose guide; the Multiplatform notes are marked in it |

## What do I need from the Tolgee Platform first?

Every guide starts from the same three things. Prepare them once per project in the Tolgee Platform; the
[Content Delivery guide](/android-sdk/content-delivery) explains each in detail.

1. **A project with your keys.** Either install the [Tolgee CLI](/tolgee-cli/installation) and push
   `res/values/strings.xml` with `format: "ANDROID_XML"`, or create the keys on the platform through the web app, the
   REST API or the [MCP server](/platform/integrations/mcp_server/about). Key names must equal the resource entry
   names the app looks up.
2. **A Content Delivery in the "Android SDK" format.** It publishes one JSON file per language at a public URL prefix.
   Tolgee Android SDK reads JSON only; a delivery in another format produces an app that silently shows bundled text.
3. **The URL prefix and the list of published languages.** Both go into `Tolgee.init` in the Application class.

## How long does it take?

With the three items above ready, the installation guides take about fifteen minutes each. The two
[starter apps](https://github.com/tolgee/tolgee-mobile-kotlin-sdk/tree/master/starters) go from clone to a
translation arriving over the air in under thirty minutes, including the platform steps, and their README is the
playbook the guides follow.

## Frequently asked questions

<FaqSection
  items={[
    {
      question: 'Can I start bundled and move to over the air later?',
      answer:
        'Yes, and it is a common path. The keys and translations already live in Tolgee, so you create a Content Delivery in the Android SDK format, add the SDK and initialize it. No key renaming and no change to your strings.xml.',
    },
    {
      question: 'Does over the air mean my app stops working offline?',
      answer:
        'No. Tolgee Android SDK caches the last translations on the device and falls back to the bundled strings.xml when the cache is empty, so a fresh install with no network still shows text.',
    },
    {
      question: 'Do I have to keep strings.xml if I create keys on the platform?',
      answer:
        'Keep it. It supplies the offline fallback and the text shown before the first response. Run tolgee pull to generate the values-xx folders from the platform so you do not maintain them by hand.',
    },
    {
      question: 'Which languages can the app show?',
      answer:
        'The languages published in the Content Delivery, listed in availableLocaleTags when you initialize the SDK. For the bundled workflow it is whichever values-xx folders you pulled before the release.',
    },
    {
      question: 'Do I need a Tolgee API key inside the app?',
      answer:
        'No. The app only uses the public Content Delivery URL prefix. API keys are used by the Tolgee CLI on your machine or in CI and must never be committed or shipped in an APK.',
    },
  ]}
/>

## Next steps

- [Content Delivery](/android-sdk/content-delivery): create the CDN endpoint and verify it with one command.
- [Tolgee with Android Views](/android-sdk/integrations/android-views) or
  [Tolgee with Jetpack Compose](/android-sdk/integrations/jetpack-compose).
- [Production guide](/android-sdk/production) before the first release.
